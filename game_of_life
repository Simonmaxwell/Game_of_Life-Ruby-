size_x = 15
size_y = 15

cell_grid = Array.new(size_x) { Array.new(size_y) }
 (0...size_y).each do |y|
   (0...size_x).each { |x|
     cell_grid[x][y] = Random.rand(2) ==1
     if cell_grid[x][y] then
       print 'X '
     else
       print 'O '
     end
   }
   puts
 end